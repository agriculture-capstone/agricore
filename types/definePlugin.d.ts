/** The root of The Agriculture Core, defined by webpack.DefinePlugin */
declare const CORE_ROOT: string;

/** The environment to run on (production or development) */
declare const NODE_ENV: 'production' | 'development';
