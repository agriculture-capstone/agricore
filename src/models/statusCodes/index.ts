/** HTTP Status Codes */
export enum StatusCode {
	// 200
  OK = 200,
  CREATED = 201,
  // 300
  NOT_MODIFIED = 304,
  // 400
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
  NOT_FOUND = 404,
  GONE = 405,
  // 500
  INTERNAL_SERVER_ERROR = 500,
}
